# Required by SEC EDGAR (identify your app/email responsibly)
SEC_USER_AGENT=StockAnalyst/0.1 (your_email@example.com)

# Backend config (UI reads this via environment or sidebar)
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
